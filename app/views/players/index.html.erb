<h1 class="center">Listing Players</h1>

<table class="round center">
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
  </tr>

<% @players.each do |player| %>
  <tr>
    <td><nobr><%= h player.name %></nobr></td>
    <td><%= link_to 'Edit', edit_player_path(player) %></td>
    <td><%= link_to 'Destroy', player, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Home', root_path %> | <%= link_to 'New Player', new_player_path%>
